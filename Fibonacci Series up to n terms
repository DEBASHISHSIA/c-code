#include<stdio.h>
int main()
{
    int n , a = 0, b = 1;
    printf("Fibonacci series up to : ");
    scanf("%d" , &n);
    
    printf("%d %d ", a,b);
    for (int i = 2; i <n ; i++)
    {
        int ans = a+b;
        printf("%d " , ans);
        a = b; 
        b = ans;
    }
    return 0;
}
